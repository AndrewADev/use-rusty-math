[project]
name = "use-rusty-math"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rusty-math",
]

[tool.uv]
find-links = ["https://github.com/AndrewADev/rusty-math/releases/expanded_assets/v0.1.0"]
